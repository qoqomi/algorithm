# 파이썬 문법 정리

## for 
for i in range(n) # 인덱스 순회 
for item in array # 값 순회 
if 3 in [1,2,3] # 포함 여부 
if not 조건 # 조건 반대 
for i,v in enumerate(array) # 인덱스, 값 동시에 
for i in range(0,10,2) # 2칸씩 건너뛴다.

## if 
if a and b      # 둘 다 True
if a or b       # 하나만 True여도
if a == b       # 같다
if a != b       # 다르다
if a is None    # None 체크 (== 대신 is 쓰는 게 맞음)